# M3 里程碑验收说明（悬浮球）

## 完成情况对照（M3 完成标准）

| 任务 | 状态 |
|---|---|
| FloatingNoteService 前台服务（通知"悬浮笔记运行中"） | ✅ specialUse 类型 + Android 13 通知权限（被拒不阻塞） |
| 悬浮球创建、拖动、点击/拖动 10dp 阈值判定 | ✅ |
| 边缘吸附 + 贴边滑出收起 + 淡出（露出 40% 直径） | ✅ 220ms 动画，DecelerateInterpolator |
| 点露出的部分唤出（滑回 + 淡入） | ✅ |
| 外观渲染管线（applyAppearance + DataStore 订阅实时刷新） | ✅ 已就绪（设置页 M5 才能改，当前用默认值：48dp 实心圆蓝球） |
| 点击悬浮球展开悬浮窗（占位） | ✅ 防重复建窗 |
| START_STICKY + onDestroy 移除全部 Window | ✅ |
| 权限引导（说明对话框 + 系统授权页 + onResume 复查） | ✅ 对话框含国产 ROM 提示文案 |
| 编译通过 | ✅ BUILD SUCCESSFUL |

新增文件：
- `service/FloatingNoteService.kt`（前台服务：球/窗生命周期、外观订阅、START_STICKY）
- `view/FloatingBallView.kt`（球：拖动、吸附、淡出、外观渲染，触摸自处理）
- `view/FloatingNoteWindowView.kt`（占位窗，M4 替换为完整结构）
- `manager/WindowManagerHelper.kt`（防重复 addView、安全 remove）
- 资源：球布局 / 占位窗布局 / 铅笔图标 / 圆角背景；Manifest 注册 specialUse 服务

## 手机验证步骤（约 4 分钟）

安装 `D:\FloatNote\app\build\outputs\apk\debug\app-debug.apk`（覆盖安装）：

**权限流程**：
1. 打开 App -> 顶栏点「悬浮窗」-> 弹出权限说明（含小米/华为提示）-> 点「去授权」-> 系统里允许"显示在其他应用上层" -> 返回 App。
2. 再点「悬浮窗」-> 悬浮球出现在屏幕右边缘偏上（首次默认位置），通知栏出现"悬浮笔记运行中"。

**拖动与吸附**：
3. 按住球拖到屏幕中间 -> 松手 -> 球自动吸附到较近的左/右边缘，并**滑出一部分只剩约 40% 露出、变半透明**。
4. 点露出的半圆 -> 球滑回完全可见、恢复亮度。
5. 把球拖离边缘松手 -> 只吸附不收起（因为不在边缘时也是吸附到最近边？注意：**当前实现是任何位置松手都会吸附边缘**，这是计划行为）。

**展开窗口**：
6. 球完全可见时点一下 -> 展开深色占位窗口（"悬浮笔记窗"，M4 说明）-> 点「关闭」关窗。
7. 快速连点球多次 -> 只出现一个窗口，不崩溃。

**开关与恢复**：
8. 顶栏点「关闭悬浮球」-> 球和通知消失。
9. 再点「悬浮窗」-> 球回来，位置是上次吸附的位置（位置记忆）。
10. （可选）在系统开发者选项里"不保留活动"或直接杀掉 App 进程再重开 -> 悬浮球由 START_STICKY 尽量恢复；国产 ROM 上可能不恢复，属预期（计划验收 26 条写明"尽力而为"）。

全部符合即 M3 验收通过。

## 已知边界（按计划设计的当前行为）

- 悬浮球颜色/大小/样式当前固定默认值（蓝色 48dp 实心圆），**改外观的设置页在 M5 提供**；M3 已实现"改设置立即生效"的订阅管线。
- 贴边收起态不跨重启：重启服务后球显示在上次吸附的"完全可见"位置。
- 首次启动会请求通知权限（Android 13+），拒绝不影响悬浮球，只是通知栏看不到"运行中"通知。
