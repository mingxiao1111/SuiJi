# M1 里程碑验收说明

## 完成情况对照

| M1 任务 | 状态 |
|---|---|
| 创建 Android 项目（minSdk 26 / targetSdk 34 / Gradle Kotlin DSL） | ✅ 完成（compileSdk 35，见下方说明） |
| 添加依赖（Compose / Room / DataStore / Coroutines） | ✅ 完成 |
| Manifest 权限（SYSTEM_ALERT_WINDOW / FOREGROUND_SERVICE / POST_NOTIFICATIONS / FOREGROUND_SERVICE_SPECIAL_USE） | ✅ 完成 |
| Note / NoteDao / NoteDatabase / NoteRepository | ✅ 完成 |
| SettingsRepository（计划 5.2 全部 18 个设置 key + 快照读取） | ✅ 完成 |
| OverlayPermission 工具类 + MainActivity 权限引导 | ✅ 完成 |
| 能编译运行 | ✅ 编译通过（APK 9.2MB）；运行验证见下方步骤 |
| 数据库可写入并读出测试笔记 | ⏳ 待手机上点按钮验证 |
| 未授权时跳系统授权页，返回后状态正确 | ⏳ 待手机上点按钮验证 |
| 全部设置 key 可读写 | ⏳ 待手机上点按钮验证 |

> compileSdk 35 说明：androidx.core 1.15 要求 compileSdk ≥ 35。compileSdk 只决定编译用的 API 版本，不影响运行行为；targetSdk 仍为 34（计划拍板值）。

## 手机验证步骤（约 2 分钟）

1. 把 `D:\FloatNote\app\build\outputs\apk\debug\app-debug.apk` 传到手机并安装（允许未知来源）。
2. 打开「悬浮笔记」App，会看到 M1 验证页，三张卡片：

**① 悬浮窗权限卡片**：显示「未授权 ✗」→ 点「去系统授权页」→ 系统设置里允许"显示在其他应用上层" → 返回 App → 状态应变为「已授权 ✓」（验证 onResume 复查逻辑）。

**② 数据库读写卡片**：点「写入并读出测试笔记」→ 应显示「读出成功：id=1，标题=M1 测试笔记，updatedAt=…」。

**③ 设置项读写卡片**：点「写入并读回全部设置」→ 应显示三行全部 18 个设置项的值（ball=… size=52 style=2 … 等，与写入值一致）。

三项都符合预期即 M1 验收通过，可以进入 M2。

## 编译命令

```bash
cd /d/FloatNote
export JAVA_HOME="D:\\java1"
cmd //c "gradlew.bat assembleDebug"   # 增量编译约 20-30 秒
```

APK 输出：`app\build\outputs\apk\debug\app-debug.apk`

## 环境备忘（后续里程碑会遇到）

- **模拟器不可用**：本机 Windows 11 build 26200 上 WHPX 虚拟化与 emulator 37.1.11 存在兼容问题（qemu 启动后 vCPU 零活动、设备永远 offline）。后续里程碑的运行验证统一走真机（USB 调试 + adb install），或装 Android Studio 用其自带模拟器再试。
- **网络代理**：系统代理 127.0.0.1:7897。curl 正常，但 Gradle 的 JVM 直连外网会失败（首次 wrapper 生成失败的原因）。依赖现已全部缓存到 ~/.gradle，后续编译不受影响；若需新增依赖，给 Gradle 加代理配置（gradle.properties 里 systemProp.http.proxyHost 等）或手动下载。
- **SDK / Gradle 位置**：SDK 在 D:\AndroidSdk（local.properties 已配置），Gradle 8.10.2 发行版在 D:\gradle-dist（wrapper 缓存已就位，gradlew 可直接用），JDK 21 在 D:\java1。
