# 浮记 v1.0 全量验收清单（M6 总回归）

> 正式包：`D:\FloatNote\app\build\outputs\apk\release\app-release.apk`（7.0MB，v1.0）
> 测试包：`app\build\outputs\apk\debug\app-debug.apk`
> 单元测试：12/12 通过（NoteTextUtils 分点/待办/缩进/时间戳全函数覆盖）

## M6 新增的加固项

| 项 | 说明 |
|---|---|
| 快速双击竖条防双窗口 | 展开进行中标志（expanding），二次点击忽略 |
| 横竖屏/分屏切换 | 窗口与竖条自动拉回可视区（onConfigurationChanged） |
| 权限被系统撤销 | 优雅退出：保存数据→移除窗口→停止服务，不闪退；回到主界面可重新授权 |
| 国产 ROM 引导 | 授权对话框含小米/华为/vivo 具体路径与电池设置说明 |

---

## 一、权限与服务

1. 首次点「悬浮窗」→ 弹权限说明（含国产 ROM 路径）→ 去授权 → 返回后自动开启。
2. 拒绝授权后再进主界面，顶栏按钮仍可重新发起授权。
3. 服务运行中去系统设置关闭"显示在其他应用上层"→ 悬浮窗消失、服务停止、无闪退。
4. 通知栏有"悬浮笔记运行中"常驻通知（拒绝通知权限时仅通知不可见，功能正常）。

## 二、主界面（列表 / 编辑）

5. 列表按今天/昨天/本周/更早分组，品牌蓝色组头；条目标题+时间同行、摘要第二行、组内细线。
6. 胶囊「＋新建」按钮；空态有图标+文案+「写第一条」按钮。
7. 编辑页：大标题输入、元信息行（时间·字数实时）、底部保存状态；1 秒防抖自动保存。
8. 列表↔编辑转场滑动动画；返回键与顶栏返回都走保存路径。
9. 长按列表项删除（确认框）；空笔记（点开即退）自动清理。
10. 杀进程重开：笔记/设置/窗口位置全部保留。
11. 深色模式：主界面、悬浮窗、竖条、工具栏配色正常。

## 三、悬浮窗与收起

12. 单击竖条展开：窗口在竖条右侧出现，把手与竖条位置衔接（从哪来回哪去）。
13. 单击把手收起：窗口左滑出→竖条滑入把手位置；振动确认（可关）。
14. 拖动窗口：左边缘出现目标区虚影→拖近点亮+振动→松手收起；拖离熄灭；普通停放不收起。
15. 快速甩动窗口向边缘→收起（彩蛋路径，可在设置关闭）。
16. 竖条：单击/左右滑唤醒；上下拖调高度；快速双击不产生双窗口。
17. 移动竖条后展开→窗口出现在竖条旁；反复收展位置不漂移。
18. 缩放：右下角手柄拖拽 + 双指捏合，最小 100×80dp；窗口贴屏幕左右边缘（边距/防误触距离按设置）。
19. 键盘：第一次点正文直接弹键盘（光标落末尾），再点定位光标；蓝色光标显示；键盘弹出窗口上移避让；返回键收键盘。
20. 文本：长按弹「全选/复制/粘贴」菜单（按状态显示），复制有提示。
21. 工具栏：窗内上/下方（可设置），9+2 个按钮显隐/大小/透明度可调；分点自动续号（回车续行、空项回车结束）；新建笔记绑定新空白页；设置/主页按钮跳转对应界面。
22. 双向同步：主界面与悬浮窗互改实时刷新；删除当前笔记窗切"已删除"态不崩溃。
23. 无操作自动收起（默认关，可开 5s/10s/30s/1min/永不）；连续打字不收起。

## 四、贴边竖条

24. 长条/半把手两形态可切（默认长条：30%/20dp/50dp）。
25. 颜色六色、透明度/粗细/高度可调，实时生效；无重影。
26. 收起固定贴左边缘；位置记忆。

## 五、异常与兼容

27. 横竖屏/分屏切换：窗口和竖条回到屏幕内，不越界。
28. 其他 App 正常使用（悬浮窗不抢焦点、不拦截返回键）。
29. 服务被杀后 START_STICKY 尽力恢复（国产 ROM 允许延迟或不恢复）。
30. 连续开关悬浮窗、快速收展，无残留窗口/竖条、无崩溃。

## 已知说明

- 系统级限制：overlay 窗口上不显示系统文本选择菜单（已用自制菜单替代）；部分 ROM 上无障碍焦点框由系统绘制。
- 本机编译提示：机器内存紧张时 Kotlin 编译守护进程可能崩溃并自动降级重试，编译变慢但产物正常；重启电脑可恢复速度。

---

**全部通过即 v1.0 验收完成。** 项目结构：`D:\FloatNote`（Kotlin + Compose + Room + DataStore，minSdk 26 / targetSdk 34）。
