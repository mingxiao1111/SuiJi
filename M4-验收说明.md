# M4 里程碑验收说明（悬浮窗完整版）

## 完成情况对照（M4 完成标准 + 用户新增需求）

| 任务 | 状态 |
|---|---|
| 标题栏拖动移动窗口 | ✅ 拖动开始自动收键盘（v2 修复回落事件绕过判定的 bug） |
| 右下角手柄缩放（最小 220×280dp / 最大屏幕 85%） | ✅ 下限已放宽，缩放更自由（v2） |
| **双指捏合缩放**（M4 用户新增） | ✅ 两指按住拉伸/捏合，实时缩放，同样受最小/最大限制 |
| **缩放手柄可设置关闭**（M4 用户新增） | ✅ `show_resize_handle` 设置项已接入（设置页 M5 提供 UI） |
| **拖到屏幕边缘收起为贴边悬浮球**（M4 用户新增） | ✅ 窗拖到左/右边缘 48dp 内松手 -> 窗收起 -> 球以贴边半透明态出现 |
| **贴边的球点击直接展开悬浮窗**（M4 用户新增） | ✅ 球任何状态点击都开窗 |
| **独立可拖动工具栏**（M4 用户新增） | ✅ 不占悬浮窗空间，默认在窗下方，可拖到任意位置，位置记忆 |
| **收起钮**（v2：右侧偏下，单手友好） | ✅ 短按收起为球；按住拖动 = 移动窗口（也可拖到边缘收起）；标题栏只留「✕」 |
| **键盘优先**（v2：修复光标/键盘冲突） | ✅ 未进入编辑时第一次点正文只弹键盘（光标落末尾），键盘在场后点击才移动光标 |
| 焦点策略（平时不抢焦点 / 返回键释放） | ✅ |
| 键盘弹出窗口上移避让（overlay 手动避让） | ✅ 可视区域差值法，收起恢复 |
| 工具栏 8 按钮（分点/待办/缩进/时间戳/字号） | ✅ 有序分点自动续号、待办 ☐/☑ 切换、时间戳 MM-dd HH:mm |
| 插入类按钮不强制弹键盘（无焦点按光标位置直接插入） | ✅ |
| 编辑自动保存（1s 防抖）+ 与主界面实时同步 | ✅ 正在输入时不替换文本，避免光标跳动 |
| 默认绑定最近编辑笔记；无笔记自动新建 | ✅ lastNoteId 持久化 |
| 删除当前笔记 -> 窗切空白提示态不崩溃 | ✅ |
| 窗口背景不透明度 / 字号设置接入 | ✅ 即时生效 |
| 深浅色适配 + 扁平化视觉（v2） | ✅ 统一底色统一圆角 16dp（标题栏不再单独铺色，圆角上下一致）、淡描边、工具栏淡边框 |

新增/重写文件：`FloatingNoteWindowView`（完整版）、`FloatingToolbarView`（独立工具栏）、`ToolbarAction`、`text/NoteTextUtils`（纯函数）+ 12 个单测、Service 状态机重写、设置项 3 个新 key。

## 手机验证步骤（约 6 分钟）

安装 `D:\FloatNote\app\build\outputs\apk\debug\app-debug.apk`（覆盖安装）：

**开窗与编辑**：
1. 开悬浮球 -> 点球 -> 展开悬浮窗（绑定最近编辑的笔记；没有笔记会自动新建一篇），窗下方出现深色胶囊工具栏。
2. **第一次点正文 -> 只弹键盘（光标落到末尾）；键盘在场后再点 -> 光标移动到点击处**；点返回键 -> 键盘收起；此时打开别的 App 仍可正常点击（不抢焦点）。
3. 打字停 1 秒自动保存；回到主 App 列表能看到这篇笔记内容已更新（时间也刷新）。

**工具栏**：
4. 在笔记里输入两行文字，光标放第二行 -> 点「1.」-> 第二行插入 "1. "；再点一次不重复。
5. 上一行是 "3. xxx" 时光标在下一行点「1.」-> 插入 "4. "（自动续号）。
6. 点「☑」-> 当前行插入 ☐，再点变 ☑，再点变回 ☐。
7. 点「⏱」-> 光标处插入 "08-26 14:30" 格式时间。
8. 点「A+」/「A-」-> 正文字号即时变化（12~24 限制）。
9. **按住工具栏空白处拖动** -> 整条工具栏跟着走，可放到屏幕任意位置；位置重启后记忆。长按任一按钮显示功能名。

**缩放与移动**：
10. 拖右下角「◢」手柄 -> 窗口实时缩放（下限 220×280dp，上限屏幕 85%，范围内任意尺寸）。
11. **两根手指按在窗上捏合/张开** -> 窗口按比例缩放。
12. **按住标题栏拖动** -> 窗口移动（拖到屏幕左/右边缘松手会收起为球）。

**收起钮（右侧偏下圆形按钮）**：
13. 短按收起钮 -> 窗收起为贴边悬浮球。
14. **按住收起钮拖动** -> 移动整个窗口（拖到边缘松手同样收起）。
15. 点标题栏「✕」关闭 -> 球完全可见态出现。

**同步与删除**：
16. 主 App 打开同一笔记改内容 -> 悬浮窗内容实时变；悬浮窗改 -> 主界面实时变。
17. 主 App 删除悬浮窗正在显示的笔记 -> 窗切"笔记已删除"提示态，不崩溃。
18. 点贴边的球 -> 窗展开（恢复上次位置尺寸）。

全部符合即 M4 验收通过。

## 已知边界（按计划/M5 待做）

- 工具栏按钮显隐、分点样式（1. / 1、/ ①）、缩放手柄开关、字号滑条、固定位置等**设置 UI 在 M5 提供**，当前用默认值。
- 双指捏合时第一根手指若按在正文上，可能同时触发一次文字光标定位，属系统手势共存，无功能影响。
