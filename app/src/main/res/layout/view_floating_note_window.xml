<?xml version="1.0" encoding="utf-8"?>
<!-- 悬浮笔记窗（v8 / v2.0 一期）：窗口宽度 = 卡片宽度 + 长条厚度，长条零间隙贴卡。
     工具栏并入窗口（上/下位置可设置）；右下角低调缩放手柄。
     背景/颜色/圆角/字号/长条厚度由代码动态设置。 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/windowRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:importantForAccessibility="noHideDescendants">

    <!-- 三段长条手柄：与卡片同高（共用 8dp 上下留白给阴影），轻点收起/按住拖动；
         宽度与卡片 marginStart 由代码随设置同步（此处初始值=出厂默认厚度 27dp） -->
    <com.zhao.suiji.view.FloatingBarView
        android:id="@+id/barView"
        android:layout_width="27dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:importantForAccessibility="no" />

    <!-- 圆角卡片：工具栏 + 正文（背景由代码设置，四周留白给 elevation 阴影；
         marginStart = 长条厚度，由代码随设置同步） -->
    <FrameLayout
        android:id="@+id/cardBody"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="27dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- 工具栏（上方形态）：分隔线 + 横向按钮排 -->
            <LinearLayout
                android:id="@+id/toolbarTopContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <HorizontalScrollView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:scrollbars="none">

                    <LinearLayout
                        android:id="@+id/toolbarTop"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal" />
                </HorizontalScrollView>

                <View
                    android:id="@+id/dividerTop"
                    android:layout_width="match_parent"
                    android:layout_height="1dp" />
            </LinearLayout>

        <EditText
            android:id="@+id/noteEditor"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@null"
            android:gravity="top"
            android:imeOptions="flagNoExtractUi"
            android:importantForAccessibility="no"
            android:inputType="textMultiLine|textNoSuggestions"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:textCursorDrawable="@drawable/cursor_note"
            android:textSize="15sp" />

            <!-- 工具栏（下方形态）：分隔线 + 横向按钮排 -->
            <LinearLayout
                android:id="@+id/toolbarBottomContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <View
                    android:id="@+id/dividerBottom"
                    android:layout_width="match_parent"
                    android:layout_height="1dp" />

                <HorizontalScrollView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:scrollbars="none">

                    <LinearLayout
                        android:id="@+id/toolbarBottom"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal" />
                </HorizontalScrollView>
            </LinearLayout>
        </LinearLayout>

        <!-- AI 入口（v2.1-a4 用户拍板）：卡片左下角半透明圆钮，与右下缩放手柄对角相对；
             随侧别镜像；显示与透明度由代码驱动 -->
        <FrameLayout
            android:id="@+id/aiFab"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="10dp"
            android:layout_marginBottom="10dp"
            android:visibility="gone"
            android:importantForAccessibility="no">

            <ImageView
                android:layout_width="17dp"
                android:layout_height="17dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_ai_atom"
                android:importantForAccessibility="no" />
        </FrameLayout>
    </FrameLayout>

    <!-- 缩放手柄（三斜线 grip；v2.0-a5 去背景圆片+低透明度，弱化为"低调提示"不抢视觉） -->
    <ImageView
        android:id="@+id/resizeHandle"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="14dp"
        android:alpha="0.4"
        android:padding="6dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_resize_grip"
        android:importantForAccessibility="no" />
</FrameLayout>
