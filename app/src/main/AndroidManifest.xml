<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 悬浮球 / 悬浮窗（系统级 overlay） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 前台服务保活悬浮球（M3 启用 service 声明） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 特殊用途前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 13+ 前台服务通知展示；被拒不阻塞服务启动 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 收起目标区 / 收起完成的触觉反馈 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 开机自启悬浮窗（v1.2，需在设置中打开） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- AI 助手（v2.1）：访问用户自配的 AI 接口；其余功能零网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 明文流量放行：仅 AI 功能联网且端点由用户自配，为局域网/本地模型（http://）留路 -->
    <application
        android:name=".FloatNoteApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- AI 选图中转（T5）：透明无界面，选完压缩入 cache 并回传 Service -->
        <activity
            android:name=".AiImagePickerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- 开机自启（v1.2）：BOOT_COMPLETED 是系统保护广播，exported 接收 -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- specialUse 类型：Android 14+ 前台服务必须声明类型与用途（计划 11.3） -->
        <service
            android:name=".service.FloatingNoteService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating note overlay window service" />
        </service>
    </application>
</manifest>
