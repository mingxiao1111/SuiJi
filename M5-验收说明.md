# M5 里程碑验收说明（设置页 + 自动收起）

## 完成情况对照（M5 完成标准）

| 任务 | 状态 |
|---|---|
| 设置页：悬浮球组（大小/样式/颜色/常态透明度/贴边淡出开关/淡出透明度） | ✅ |
| 设置页：悬浮窗组（背景不透明度/缩放手柄开关/固定位置/自动收起开关+时长） | ✅ |
| 设置页：输入与格式组（字号/工具栏 9 按钮显隐/有序样式/无序样式） | ✅ |
| 设置页：重置（窗位置大小/工具栏位置/球位置）+ 关于（版本号） | ✅ |
| 设置实时生效（球外观/工具栏/字号/窗属性/把手颜色） | ✅ Service 订阅 DataStore |
| 自动收起计时：三源重置（触摸+打字+工具栏点击） | ✅ AutoCollapseManager |
| 自动收起时长 5s/10s/30s/1min/永不 | ✅ 0 = 永不 |
| 所有设置重启后保持（DataStore） | ✅ |
| 编译 | ✅ BUILD SUCCESSFUL |

新增/修改：`manager/AutoCollapseManager`、`ui/SettingsScreen`、`SettingsActivity`、主界面顶栏「⚙」入口、Manifest 注册。

## 手机验证步骤（约 6 分钟）

安装新 APK。主界面右上角新增「⚙」进入设置页。

**悬浮球组**（开着悬浮球改，肉眼可见实时变化）：
1. 拖"大小"滑条 36→64 -> 屏幕上的球实时变大变小。
2. 切"样式"四次 -> 实心圆/磨砂/方块/极简圆点（圆点无图标且更小）。
3. 色板选白色 -> 球变白、图标变黑（浅色球自动配深色图标）；把手同步变色。
4. 调常态透明度 -> 球变半透明；关"贴边淡出" -> 拖球贴边不再滑出淡出（保持完全可见）。
5. 调淡出透明度 -> 贴边后的球更透/更实。

**悬浮窗组**：
6. 开悬浮窗 -> 调"背景不透明度" -> 窗底色实时变淡。
7. 关"显示缩放手柄" -> 右下角「◢」消失（双指缩放仍可用）。
8. 开"固定位置" -> 球和窗都拖不动。
9. 自动收起设 5 秒 -> 开窗静置 5 秒自动收回为球；**连续打字 10 秒不收起**（三源重置生效）；停手 5 秒收起。设"永不"则不再自动收。

**输入与格式组**：
10. 字号滑条 -> 悬浮窗和主 App 编辑页字号同步变（12~24 限制）。
11. 有序样式切「1、」-> 工具栏点「1.」插入的是 "1、"；回车续行出 "2、"。
12. 关掉"有序分点"按钮 -> 工具栏上它消失；全部关掉工具栏整行隐藏。
13. 关掉"新建笔记" -> 工具栏加号消失。

**重置组**：
14. 拖乱窗/工具栏/球位置 -> 点对应重置项 -> 各回默认位置。
15. 杀掉 App 重开 -> 所有设置保持。

全部符合即 M5 验收通过。剩余 M6 为打磨与总回归。
