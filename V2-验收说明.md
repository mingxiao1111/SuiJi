# V2 验收说明 —— v2.0 真机专项清单（三期交付）

> 版本 2.0-a7.7（versionCode 20，release 包已出）。MuMu 已回归：收展动画逐帧检测无双竖状物同帧、
> 左右换边往返、工具栏 fit、多轮稳定循环、零崩溃；出厂默认已定稿（窗宽最大/高210dp/
> 柔光/长条30%/贴边/应用名"随记（悬浮笔记）"）。以下为**必须真机确认**的专项，
> 全部通过后合入 master 并打 v2.0 标签。

## 〇、真机重点复核（a7.1~a7.7 修复项）

0. **收起闪帧是否消失**（a7.3 根治：拆窗前清空绘制底稿；连续收起 10 次，不应见"整块窗+长条"闪现一帧）。
0bis. **键盘上浮**（a7.5 终局）：窗放屏幕下半 → 点正文 → 窗应**一次性**浮到键盘上方、稳定不跳不闪；打字/展开候选栏跟随；收键盘约 1 秒内回落。

## 一、收展动画（a7 极简顺序方案，重点感受）

> a6 的形变/交叉溶解在真机与模拟器上反复出现虚影重影，a7 已整体改为**顺序淡入淡出**：
> 收起=整窗朝所在侧轻移并淡出（约 180ms）→ 完全消失后竖条在长条中心位置原位淡入；
> 展开=竖条即刻消失+整窗原位淡入。两形态任何时刻不会同屏，结构上不存在重影来源。

1. 点长条收起：整窗向贴边一侧轻轻滑移并淡出，随后竖条在原长条中心高度淡入，衔接应顺滑不生硬。
2. 点竖条展开：竖条即刻消失，整窗在原位淡入浮现。
3. 若觉得淡出方向/距离/时长（180ms）不合适，记录感受（可调）。
4. 右侧模式重复 1-3（滑移方向应朝右）。
5. 连续快速收展 5 次无残影、无卡死、无双竖条。

## 二、质感（三期，需挑选拍板）

6. 设置→悬浮窗长条手柄→质感，切换 **标准 / 高光 / 柔光** 三档，长条与竖条应同步变化；
   挑一版告诉我固化为出厂默认。
7. 圆角默认已改 13dp，配合厚度 27 / 长度 75% 确认整体观感（这三个滑条可再微调，定稿后写进文档）。
8. 竖条质感应与长条同语言（垂直渐变+同描边），确认两形态视觉统一。

## 三、IME（遗留修复，真机必测）

9. 窗口在屏幕下半，点正文：窗口应平滑浮到键盘上方（多轮重算）。
10. **用输入法自带方式收键盘（如按输入法上的收起键/滑动关闭），窗口应在约 1 秒内自动回落**——
    这是 a6 新修的回落逻辑；v1.x 会停在上浮位不动。
11. 键盘弹出状态下点工具栏 ⇄ 换边：键盘保持、窗口换侧后仍避开键盘。

## 四、镜像系统回归（二期）

12. 工具栏 ⇄：整窗滑到对侧、长条/缩放手柄/工具栏顺序全部镜像；右侧模式拖左下角缩放，
    右缘应钉住、向左拖变宽。
13. 设置→悬浮窗→贴边方向：与 ⇄ 等效（收起态改，竖条换侧滑入）。
14. 重启 App 侧别保持。

## 五、长时稳定性

15. 日常使用半天以上：悬浮体不消失、不卡死；若再现"输入法吃返回键后窗口停上浮位"（应在 1 秒内回落）请记录场景。
16. 开机自启（v1.2 功能，设置开+悬浮窗权限在）：重启手机后悬浮窗自动出现。

## 已知环境差异（不算 bug）

- MuMu 模拟器：页面导航期间服务偶发静默死亡（环境问题）；真机不应出现，若出现请记录。
- MuMu 模拟器：长条半透明曾出现竖向浅带（已修，真机本就无此现象）。
