# AGENTS.md — 浮记（FloatNote）项目导航

> 给 AI agent 的快速上手手册。人类用户是项目所有者（非程序员），所有交互用中文。

## 项目是什么

安卓悬浮笔记 app「浮记」：主界面（列表/编辑）+ 悬浮窗系统（贴边竖条 ↔ 可缩放笔记窗）。**v1.0 已完成**（M1-M6 全里程碑，2026-08-29 出 release 包）。**v2.0 已完成开发并经用户真机验收，已合入 master 并打标签 `v2.0`（2026-09-07）**（一期=长条手柄、二期=左右镜像、三期=动画质感；迭代细节见下方第 10~14 条，设计定稿见 `V2-设计说明.md`，验收清单见 `V2-验收说明.md`；标签 `v1.2` 保留原版可回滚）。

## 构建环境（原开发机实录，含多个坑，供复现参考）

```bash
cd <仓库根目录>
export JAVA_HOME=<JDK 17+ 路径>                       # 用确定版本的 JDK，PATH 里的可能不可靠
cmd //c "gradlew.bat assembleDebug --console=plain"   # Windows；增量编译约 1-3 分钟
```

- Android SDK 经 `local.properties`（**不入库**，各自配置 `sdk.dir`）指定；Gradle 8.10.2 wrapper。
- **运行验证建议用 MuMu 模拟器**（Android 15，Android 15 上一切正常）：`adb connect 127.0.0.1:16384` 后即可 `install`/`exec-out screencap -p`/`input tap` 自动化；悬浮窗权限用 `adb shell appops set com.zhao.suiji SYSTEM_ALERT_WINDOW allow` 免手动。注意 Git Bash 会转译 `/sdcard` 路径，adb shell 远端命令要整体加引号。原开发机的 AVD/qemu 机器级不可用（vCPU 零执行，穷尽排查后放弃），重要回归以真机为准，产物在 `app/build/outputs/apk/`。
- **代理环境注意**：若本机走系统代理，Gradle 的 JVM 直连外网可能失败；首次拉依赖需为 Gradle 配置代理。
- **内存紧张机器**：Kotlin 编译 daemon 偶尔崩溃（日志有 "Failed connecting to the daemon"），会自动降级重试、产物正常；若见"整个包 unresolved"级联错误，`gradlew --stop` 后单任务重跑即可。不要并行跑多个 gradle 任务。
- minSdk 26 / targetSdk 34 / compileSdk 35（androidx.core 1.15 要求，勿降）。

## 技术栈与关键拍板（勿推翻）

- Kotlin + **主界面 Jetpack Compose** / **悬浮窗传统 XML View**（拍板：overlay 窗口用 View 最稳，不混用）
- Room（notes.db **v3**，迁移纪律：改实体必须 version+1 并补 Migration，禁止破坏性重建）+ DataStore Preferences（文件名 `float_settings`）+ Coroutines/Flow
- 单 Activity（列表↔编辑用内部状态 + AnimatedContent 转场）+ SettingsActivity
- 单进程：`FloatingNoteService`（前台服务 specialUse）持有全部 overlay 窗口
- 触摸逻辑全部在 View 内自处理，Service 只管生命周期与设置分发

## 文件地图（核心）

```
app/src/main/java/com/example/floatnote/
├── MainActivity.kt / SettingsActivity.kt     # 入口 + 权限引导对话框（含国产 ROM 文案）
├── FloatNoteApp.kt                            # Repository 单例容器
├── service/FloatingNoteService.kt             # ★ 状态机：竖条↔窗口互斥、收起/展开锚定（长条中心=竖条中心）、振动
├── view/FloatingBallView.kt                   # ★ 贴边竖条（v2.0 起仅长条形态，渐变+滑入动画）
├── view/FloatingNoteWindowView.kt             # ★ 悬浮窗（长条拖动/箭头收起/双指缩放/键盘优先/
│                                              #   IME 避让(三轮重试)/自制文本菜单/回车续点/收起滑出动画）
├── view/FloatingBarView.kt                    # ★ 三段长条手柄（v2.0：上换位/中拖动/下收起，分区+保底28dp）
├── view/ToolbarAction.kt                      # 工具栏按钮枚举（11 个，含设置/主页）
├── text/NoteTextUtils.kt                      # 纯函数：分点/待办/缩进/时间戳（12 个单测覆盖）
├── data/SettingsRepository.kt                 # ★ 全部设置 key/flow/setter + FloatSettings 快照
├── data/BackupFormat.kt                       # 备份文本格式（纯函数，build/parse 互通，5 个单测）
├── manager/                                   # AutoSaveManager(1s防抖) / AutoCollapseManager(三源重置)
└── ui/                                        # Compose：列表/编辑/回收站/设置页 + theme（v1.0.1 起黑白中性系，无 Color.kt，全在 Theme.kt）
app/src/main/res/layout/view_floating_note_window.xml  # 窗口布局 v8（长条贴卡零间隙 + 卡片margin 8dp 给阴影）
app/src/test/.../NoteTextUtilsTest.kt          # 单元测试
```
（★ = 改动最频繁的四个文件，先读它们）

## 核心交互模型（改行为前必须理解）

1. **两形态互斥**：贴边竖条（收起态）↔ 悬浮窗（展开态），Service 里 `ballView` / `windowView` 二选一。
2. **从哪来回哪去（v2.0 一期起）**：展开时窗口锚定竖条位置（**长条中心=竖条中心**，取代 v1 的"把手顶=竖条顶+18dp"）；收起时竖条中心落在长条中心高度并写入 ballY；两边位置互相衔接，模型统一。
3. **收起两入口（v2.0 一期定稿）**：长条下段收起箭头单击（主路径）/ 超时自动（默认关）。~~拖入目标区~~ / ~~单击把手~~ / ~~快速甩动~~ 三条手势收起已整体删除（手机端误触根因，V2-设计说明 §2.2），拖动从此只移窗。竖条形态仅剩长条（半把手款废弃）。
4. **键盘优先**：未聚焦时第一次点正文只弹键盘（onInterceptTouchEvent 路径，光标落末尾），再点才定位光标。历史 bug：曾因劫持 dispatchTouchEvent 触发系统绿框，勿回退。
5. **窗口宽度 = 卡片宽 + 长条厚度（v2.0-a3 一体式）**：长条（FloatingBarView）零间隙贴卡、**不分段无分界线**——轻点任意处收起、按住拖动移窗，换位键在工具栏（不在长条上）；可见高度占窗高 75%（可调 50-95）居中缩短，触摸热区仍全高；**渐变必须垂直方向**（TL_BR 对角渐变在厚条上产生竖向色带=厚度28"竖线"bug）；圆角可设置默认 10dp（渲染 clamp 半厚防胶囊）。
6. **设置全参数化**：竖条/长条/窗口/输入格式/工具栏等分组，DataStore 驱动实时生效；滑条**不显示刻度点**（a3 拍板，SliderRow 内部 steps=0+回调量化保离散步进，跨档振动反馈）；设置页为可折叠抽屉卡片（默认收起）。改某项范围时同步改 SettingsRepository setter 的 coerce 和 SettingsScreen 的 min/max/steps。v2.0 废弃项：把手宽/高（宽映射为长条厚度）、握线四样式、半把手形态、感应区宽度、甩动开关、三段式中段占比（a2 改两段式时废弃）、两段式上段占比+分界线（a3 回归一体时废弃）。
7. ~~握线四样式~~（v2.0 废弃，长条语言由图标+三段结构表达）。
8. **回收站与备份（v1.1）**：列表长按/编辑页删除→软删除进回收站（顶栏入口，30 天自动清、可恢复/彻底删除/清空）；设置页"备份"分组可导出全部笔记为纯文本（SAF 免权限）并从文件导回（跳过重复）。
9. **搜索/置顶/开机自启（v1.2）**：顶栏搜索图标进入搜索态（标题+正文不区分大小写，结果扁平展示）；长按笔记弹操作单（置顶/取消置顶 + 移入回收站），置顶组固定在最前、条目时间带"置顶 ·"前缀；设置→悬浮窗→"开机自启悬浮窗"开关（BootCompletedReceiver，开关开+悬浮窗权限在才拉起服务）。注意：MuMu 平台会拦截第三方应用的 BOOT_COMPLETED 投递（dumpsys 可见 "blocked by mumu"），开机自启只能真机验证；系统"强制停止"过的应用也收不到开机广播（Android 通用限制）。
10. **v2.0 一期（2026-08-31，分支 v2-redesign）**：三段长条手柄替代球形把手（详见 `V2-设计说明.md`）；尺寸口径统一为"卡片"宽高（修 v1.2 存总宽取当卡片宽导致反复收展变宽的隐患）；IME 避让加两轮重试（250/700/1400ms，真机键盘上浮失效的疑似根因是单次检查错过时机，**真机验收必测**）；Service 留有 V2Anchor 锚定日志供真机验收采证。换位键一期为 Toast 占位（"二期开放"）。已知遗留：输入法吃掉返回键时窗口保持上浮位、编辑焦点不释放，属 v1.x 既有行为，列入真机验收观察项。
11. **v2.0-a2/a3（2026-09-01，用户真机反馈两轮改版）**：a2 三段式→两段式+分界线；a3 又**回归一体式**（轻点收起+按住拖动，换位键挪到工具栏第二个位置、Toast 占位二期），渐变改垂直方向（修厚条竖线 bug），圆角参数化默认 10dp，出厂默认厚度 27dp/长度 75%；竖条按下振动、设置滑条跨档振动+去刻度点。**MuMu 平台重要事实**：本机 overlay 窗口 params.y 与屏幕渲染坐标存在约 56px（状态栏高）统一偏移，窗口/竖条两 overlay 同偏移故锚定自洽，但**像素级验证截图时必须以窗口实际渲染位置校准**（V2Anchor 日志的 yPx ≠ 截图坐标）。**开发陷阱三则**：①XML 引用的自定义 View 必须带 @JvmOverloads(Context, AttributeSet) 构造器否则 inflate 必崩；②XML 写死的初始尺寸必须与代码默认值同步（applyBarStyle 一度只在"厚度变化"时重排，attach 时字段=默认值跳过重排导致 18dp 旧布局残留，已改无条件重排）；③**逐像素 ARGB 半透明渐变在模拟器合成路径上会出现通高竖向浅带**（a3.1 实测消元：图标/描边/渐变内容/软件层均无关，透明度=1 即消失）——修法：drawable 用不透明色、半透明走**视图整体 alpha**（FloatingBarView.renderBar），按压/回弹动画的 alpha 要以 barAlpha 为基准取相对值。
12. **v2.0-a4 镜像系统（2026-09-02，二期）**：Side 枚举（data 包）+ DataStore key `side`；Service 侧别缓存为唯一真源（observeSide 首发射只同步、设置页改「贴边方向」等效 performSwap）；工具栏 ⇄ 键=整窗翻到屏幕中线对称位（250ms）+内部镜像（长条/缩放手柄/卡片边距 applyMirrorLayout、工具栏反转 toolbarActionsOriginal 重排）；竖条 attach 带侧别（edgeX/滑入方向/屏变回弹）；收起滑出方向随侧。**附带修复**：编辑器未聚焦时工具栏第一击被键盘优先误拦（v1.x 遗留，工具栏容器已加入 onInterceptTouchEvent 排除清单）。**MuMu 环境观察项**：模拟器上应用页面导航期间服务偶发静默死亡（无崩溃无系统日志，纯 overlay 交互从不复现），测试循环遇到"悬浮体消失"先查 dumpsys 服务再下结论。
13. **v2.0-a5（2026-09-02，用户真机反馈四条）**：①右侧模式缩放修复——resizeTo 改右缘锚定（拖左下角时右缘钉住往左收放），缩放手柄 x 轴语义取反（向左拖=变宽）；缩放手柄去背景圆片改 40% 透明度纯图标②工具栏重做——删"设置"按钮（枚举/常量/分支整体移除），默认 7 个（新建/⇄/有序/无序/待办/时间/主页），**fit-to-width**：按钮总宽超卡片可用宽时整体缩小到恰好放下（toolbarEffScale，窗口尺寸变化经 onSizeChanged 120ms 去抖重排），永不出现半个按钮；此改动顺带解决了 a4 记档的"右侧模式 ⇄ 被滚出可视区"（7 按钮不滚动，反转后仍全可见）③长条收起箭头描边 1.5→2dp（用户嫌细）④竖条滑入动画稳化（160→200ms、起点贴屏外 2px，减弱减速尾段的"回弹感"）。**测试注意**：MuMu 上 adb 点工具栏按钮的 y 坐标会因按钮 fit 缩小而失准（按钮高度随 effScale 变），自动测试点位要以像素聚类实测为准。
14. **v2.0-a6 三期·形变与质感（2026-09-02）**：①**收起/展开形变动画**取代滑出+滑入——morphCollapse：卡片向长条侧收拢淡出、长条撑满窗高后收缩成竖条形，末段透明度衔接竖条 fadeAlpha，末态=真竖条初态（实测中心偏差<2px）；animateExpandFromStick 严格反向；形变期间 morphing 标志吞触摸②**质感系统**：BarTexture 枚举（标准/高光/柔光），设置页「质感」三选一（长条与竖条同步），真机挑选后待固化默认；圆角默认 13dp 定稿；竖条渲染与长条统一（垂直渐变+同描边）③**IME 回落修复**：recheckImeShift 双向化——键盘在则上浮、消失则回落。**a6.1 真机反馈修正（2026-09-03）**：形变改**纯视图变换**（scale/translation 动长条视图、窗口几何全程不动——逐帧改 WindowManager 尺寸在真机合成器上拖影/虚影；展开初态在 post 布局后首帧前摆好，修"先闪全宽"）；形变落位按屏幕坐标换算（screenOffset：params 坐标与渲染坐标的 ROM 级偏移）；**clampX 按侧钳制长条所在缘**（右模式 x≤屏宽-边距-窗宽，修"右侧长条可拖出屏幕"——注意钳的是右缘不是左缘上限，写反过一次）。版本 2.0-a6.1/11。**a6.2→a7 动画终局（2026-09-03/04，用户反馈交叉溶解仍有重影且方案复杂笨重）**：a6.2 平移+透明度交叉溶解（窗淡出与竖条淡入同帧交叠）在模拟器上仍被看出"两个竖状物"；a7 彻底改**极简顺序方案**——收起=整窗朝所在侧轻移 40px+淡出（180ms，单一表面纯 alpha/translation）→完全消失后拆窗挂竖条原位淡入（150ms）；展开=竖条即拆+整窗同位淡入；**两形态永不同屏**，结构上无重影来源。已删除 a6 系全部形变辅助代码（morphCollapse/animateExpandFromStick/resetMorphTransforms/screenOffset/FloatingBarView.morphToFullLength 等）。**MuMu 判重影方法论（重要，复用）**：录屏抽帧逐列扫"半透明竖状物"区段，区分**重影**与**穿透**——穿透物（透过半透明卡片看到的背景列表文字/勾选框）特征：高度不匹配任何悬浮体、卡片不透明后消失、与悬浮体位置相邻接成一连续带；真重影则是与悬浮体同形但位置偏移的分离物。视觉模型审帧会被穿透物误导，须以像素证据裁决。**a7.1 闪帧修复（2026-09-05，用户反馈收起后闪一帧"弹出"、展开正常）**：根因=新挂载 overlay 的 Surface 首帧合成可能赶在 alpha=0 生效前、以终值透明度先显一帧——修法是**挂载前置零**：竖条淡入封装进 FloatingBallView.attach（fadeIn 参数，addView 前 alpha=0 再原地淡入 150ms）、窗口在 Service addView 前预设 alpha=0，两形态同源；顺手按动效语言调插值器（展开 200ms 减速浮现/收起 180ms 加速离场/竖条淡入减速）。**a7.2 两修（2026-09-06，用户真机反馈：收起仍闪一帧 + IME 上浮从未生效）**：①**闪帧真根因=长条松手的 Overshoot 回弹**——轻点收起时 ACTION_UP 同时启动长条过冲回弹（scale 0.94→1.06→1.0，240ms）与整窗淡出（180ms），两动画并行约 120ms，肉眼即"闪一下弹出"（a7.1 修的 Surface 首帧竞态真实存在但非主因）；修法：轻点收起路径取消子视图动画静默复位，拖动结束保留果冻回弹。**教训：闪帧未必在挂载，先查收起触发路径上的并行子视图动画**。②IME 上浮根治——原纯 visibleDisplayFrame 差值法在多数国产 ROM 上对 overlay **恒返回全屏**（差值恒 0，真机从未触发、MuMu 恰好正常故一直没暴露），改双信号：API30+ 直读 rootWindowInsets 的 ime 高度（overlay 不参与协商但 insets 快照含真实状态）+差值法回退，另加编辑焦点期 350ms 轮询（beginEdit 启动/endEdit/detach 停止）全程跟随键盘显隐。**MuMu 环境新事实**：分辨率/方向跟随宿主窗口形状，adb 测试若坐标全错先 `wm user-rotation lock 0`+`wm size` 锁定；MuMu 内仅搜狗输入法且对 adb 场景 showSoftInput 不响应，**IME 避让只能真机验证**；主页顶栏"悬浮窗"是文字按钮（TextButton）不是图标。**a7.3 收起残影闪帧终局（2026-09-06，用户提供决定性特征"弹出的是整块窗+长条、在消失之后竖条之前"）**：根因=**拆窗瞬间合成器重播窗的绘制底稿**——alpha 淡出只是合成时打折，窗 buffer 里始终是完整亮度的卡片+长条；removeView 时部分合成器按默认状态把底稿再投一帧（真机 100% 复现，MuMu 上极短难截）。修法：**拆窗前先把窗标记 INVISIBLE（跳过绘制=底稿清空），postDelayed 32ms 等两帧空底稿进合成管线再拆**——重播也只能播到空画面；竖条出现晚约 32ms 无感。**教训（a7 顺序方案的盲区）："两形态不同屏"防的是重影，防不了"已消失物体复活一帧"——overlay 的 alpha 动画不擦 buffer，拆窗前必须显式清空绘制内容**。**a7.4 IME 上浮不足修复（2026-09-06，真机反馈"上浮了但高度不够仍被键盘挡"）**：根因=旧 shiftUp 的 `imeShift==0` 守卫让上浮只执行一次永不修正，而键盘弹出后 250ms 首查常落在键盘动画中间态（真机动画 300ms+，insets 高度是过程值），按半张键盘让位后轮询被守卫挡住；修法=改"持续对齐"语义——键盘在时每次以未上浮基准位 baseYBeforeShift 按**当前**键盘高度重算目标、差则补移（轮询 350ms 自动收敛，候选栏展开也跟随），另留 8dp 间隙不贴死键盘顶。**a7.5 IME 避让终局（2026-09-06，真机反馈 a7.4"一直闪"=窗上下反复跳）**：根因=**insets 的 ime 高度语义是"键盘与本窗的交集"而非键盘全高**——窗抬到位后交集归零，用它判"键盘没了"就回落，回落后又重叠再上浮，350ms 一循环；a7.4 放开修正后循环转起来（a7.4 前的"只动一次"守卫歪打正着挡住了循环，表现为高度不够）。终局结构：**键盘在否用 `rootWindowInsets.isVisible(Type.ime())`（全局显示状态，与窗口位置无关）门控回落；让多少用交集高度驱动增量上移（被挡就让开+8dp，没被挡一动不动）**——键盘弹着期间回落通道锁死，窗只可能单调抬到位后静止，结构上无震荡；键盘变高自动补齐。教训：**insets 高度对 overlay 是交集语义（位置依赖），不能当"键盘在"的判据，更不是键盘全高**。**a7.6 出厂默认值定稿（2026-09-06，用户拍板五项）**：①窗口宽 300→240dp（悬浮笔记更窄）②质感默认柔光（DEFAULT_BAR_TEXTURE=2，Flow 读取与快照两处）③长条透明度 0.7→0.3（与竖条一致）④窗口距屏幕边缘 6→0dp（贴边）⑤应用名"悬浮笔记"→"随记（悬浮笔记）"（strings.xml，通知渠道/标题同步）。**测试注意：长条透明度 0.3 下像素检测阈值要放宽**（长条亮度约 179，find_bar 用 l<125 会抓不到，改 150<l<210）。**注意出厂默认改的是"未设置时读取值"，老用户存储里的旧值不受影响（除质感外无迁移）**。**a7.7 默认窗尺寸再调（2026-09-06，用户拍板）**：宽 240→**600dp**（"尽量大"——600 远超各机型上限，attach 时 `coerceIn(minW, maxW)` 自动钳到该机实际最大 `min(屏宽×0.9, 屏宽-48dp手势区-27dp长条)`，一个常量适配所有屏幕）；高 420→**210dp**（原一半）。实测卡片 575dp 顶满上限、总高 210dp。版本 2.0-a7.7/20。

## 已知平台限制（勿当 bug 修）

- overlay 窗口上系统的文本选择菜单不显示 → 已用自制长按 PopupMenu（全选/复制/剪切/粘贴）替代；v1.0.1 又接管了选区 ActionMode（进入选中即弹自制菜单）。注意：Android 14+ 部分 ROM（如 MuMu/Android 15）系统选择工具栏在 overlay 上**能**显示，此时会与自制菜单并存，属预期兜底行为。
- overlay 窗口不参与 IME insets 协商 → 手动可视区域差值避让（shiftUpForKeyboard）。
- `softInputMode` 对 overlay 无效；返回键仅在窗口持有焦点时收到（编辑态收键盘用）。

## 开发约定

- 注释写"为什么"；中文文案；SIMPLIFIED 标记用于计划外简化。
- 每次改完必须 `assembleDebug` 通过再交付；动到 NoteTextUtils/BackupFormat 必跑 `testDebugUnitTest`。
- **数据库迁移纪律（v1.1 立）**：改 Note 实体必须 version+1 + Migration（参照 NoteDatabase.MIGRATION_1_2），用户笔记是不可再生数据，永远不许 fallbackToDestructive。
- **删除语义（v1.1 起）**：用户可见删除一律软删除进回收站（30 天，App 启动自动清过期）；物理删除仅限回收站操作和空笔记退出清理。
- 单 Activity 加新 Screen 后记得挂 `BackHandler`，否则系统返回键直接退出应用（TrashScreen 踩过）。
- 交付后请用户真机验证，验收文档在项目根：`M1-验收说明.md` ~ `M6-验收说明.md`（M6 是 30 项全量清单）。
- 用户的调优出厂默认值（2026-08-29 拍板）：把手黑/70%/渐变100%/43×55、竖条30%/20/50、感应5、边距6、字号16、工具栏100%/60%、自动收起关——改默认值需用户同意。v1.0.1 拍板（2026-08-30）：整体改黑白中性风（弃蓝），竖条默认色 #4A90D9→#2E2E2E（存储中的旧默认值读取时自动迁移），列表灰底白卡，拖动指示器隐藏。

## Git

仓库已初始化（本文件同一次提交）。分支 master，提交信息用中文、一段话说清"改了什么+为什么"。
