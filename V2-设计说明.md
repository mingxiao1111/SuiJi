# V2 设计说明 —— 三段长条手柄 + 双侧镜像（v2.0 重构）

> 2026-08-31 定稿讨论第二轮收敛。⚠ 标记 = 待用户最终拍板项。
> 版本策略：原版代码以 git 标签 `v1.2` 存档（提交 a45b548），v2.0 在分支 `v2-redesign` 上开发，
> master 保持 v1.2 不动；新版效果实测对比后再决定是否合入。

## 1. 背景与痛点（为什么重构）

手机端实测暴露三个问题，均已在代码中定位根因：

1. **误触/收不起**：甩动收起只看横向速度大小、不看方向占比（向下快拖只要带一点左向分量就误收）；
   感应区只看窗口左缘离屏幕的横向距离（≤5dp），而窗口平时就停在 6dp 边距上，竖直拖动漂 1dp 即误收；
   反过来慢速斜向拖动两条路都不命中、收不起来。
2. **间隙/窗窄**：窗口总宽 = 卡片宽 + 40dp 外挂区，把手在外挂区内居中悬浮、与卡片有缝；
   这 40dp 全部从正文宽度里扣除。
3. **只能贴左**：竖条、展开锚定、目标区、缩放手柄全部左锚定硬编码（M5.6 旧拍板）。

**v2.0 的答案：把「手势猜意图」换成「按钮表意图」，把左锚定换成侧别泛化。**

## 2. 交互定稿

### 2.1 一体式长条手柄（替代球形把手；2026-09-01 a3 定稿：回归一体）

> 迭代记录：a1 三段式（上换位/中拖动/下收起）→ a2 两段式（+分界线）→
> **a3 回归一体**——用户反馈"分段破坏一体感"，换位键挪到工具栏，长条一整条不分段。

形态（左模式示意，右模式完全镜像）：

```
┌────┐┌──────────────────┐
│    ││                  │
│    ││                  │
│ ◄  ││     正文卡片      │ ← 长条一体无分段，居中一个收起箭头
│    ││                  │
│    ││                  │
└────┘└──────────────────┘
```

- **轻点长条任意处 = 收起、按住拖动 = 移窗**（触摸热区全窗高，含上下留白）；
  换位键（⇄）在**工具栏**第二个位置（二期接镜像功能，当前 Toast 占位）。
- 可见高度只占窗高一部分（**默认 75%**，可调 50%~95%），居中缩短、上下留白。
- 圆角**可设置**（默认 10dp，0~16dp；渲染时自动防超过半厚）。
- 渐变为**垂直方向**（顶受光→底背光）：a2 的对角渐变在厚条上产生竖向色带
  （厚度 28dp 时中间出现"竖线"），垂直渐变左右均匀、立体感保留。
- **半透明走视图整体 alpha**（a3.1）：逐像素 ARGB 半透明在模拟器合成路径上会出现
  通高竖向浅带（"细长条"bug），drawable 用不透明色绘制、透明度设在视图级即消失。
- 按压瞬间轻微下缩 + 振动，松手果冻回弹；**竖条按下同款振动**（a3 拍板，手感统一）。

### 2.2 收起模型（入口从四条砍到两条）

| 入口 | v1.2 | v2.0 |
|---|---|---|
| 下段箭头单击 | — | **主路径** |
| 单击把手 | 有 | 删除 |
| 拖入边缘感应区松手 | 有 | 删除（连同感应区代码与设置项） |
| 快速甩动 | 有 | 删除（连同开关设置项） |
| 超时自动收起 | 有 | 保留（照旧） |

拖动从此**只移窗、永不收起**——手势歧义的源头被整体移除，
原计划的 v1.2.1 误触判定热修也因此不再需要。

- 收起落位：竖条中心 = 长条中心高度，贴当前侧边缘。
- 收起动画（形变）：卡片淡出并向长条一侧收拢；长条高度收缩为竖条高、厚度收缩为竖条宽；
  约 250ms、先快后慢；展开动画为严格反向。
- 竖条仅保留长条形态（**半把手款废弃**），视觉与长条统一（同渐变黑）。

### 2.3 镜像系统（v2.0-a4 已实现；目的：单手操作）

- 新增全局「侧别」状态（左/右），**持久化**（key: side）；设置页「悬浮窗→贴边方向」选项，
  与工具栏 ⇄ 键等效（observeSide 首发射只同步缓存，变更触发 performSwap）。
- 交换键行为：**整窗翻到关于屏幕中线对称的位置**（250ms 动画），同时长条换到窗另一侧、
  竖条改贴对侧边缘；收起态换边 = 竖条换侧滑入。
- **键盘保持**：换边不动焦点；动画结束重算 IME 避让。
- 镜像范围：长条所在侧、竖条贴边侧、展开锚定（x=屏宽-边距-总宽）、缩放手柄（右下↔左下）、
  工具栏按钮排列顺序、收起滑出方向（向所在侧屏幕外）。
- **不镜像**：正文阅读方向（永远从左到右）、笔记内容、输入格式逻辑。
- ⚠ ~~已知取舍（待用户拍板）：10 个可见按钮超出卡片宽、工具栏横向滚动，右侧模式反转后
  ⇄ 排到第 9 位被滚出可视区~~ → **a5 已解决**：默认按钮精简为 7 个 + fit-to-width 自适应
  （按钮超宽时整体缩小到恰好放下），工具栏不再滚动，两侧模式全部按钮可见。
- v2.0-a5 增补：设置按钮从工具栏移除（不需要）；右侧模式缩放以右缘为锚、手柄 x 轴取反；
  缩放手柄改纯图标 40% 透明度；长条收起箭头 2dp；竖条滑入动画放缓。
- 附带修复（a4）：编辑器未聚焦时工具栏第一击被"键盘优先"误拦（v1.x 遗留）——工具栏/
  缩放手柄现已在拦截排除清单，第一击直达。

### 2.4 键盘上浮（按 bug 处理，2026-08-31 更正）

「窗口靠下唤起键盘时自动浮到键盘上方」：v1.0 写过一版避让代码
（IME 可视区差值 + 250ms 单次检查），但**用户真机上从未生效**——判定为真机失效的 bug，
不是"已有能力"。v2.0 重做：

1. 输入期间多次重算避让（解决键盘弹出时机/动画时长被单次检查错过的问题）；
2. 若可视区差值方案在真机 ROM 上不可靠，改用零尺寸 PopupWindow 测量键盘高度的成熟方案
   （悬浮窗类应用通用做法）；
3. 上浮/回位平滑动画；换边后重算；
4. 真机专项验收——MuMu 模拟器无法代表真机 ROM 的 insets 行为，此项必须真机确认。

## 3. 视觉方向（无参考图，按描述出稿）

- 质感关键词：**立体流体 / 玻璃感渐变黑**。实现方向：对角线渐变（受光→背光）
  + 顶缘 1dp 高光描边（玻璃反光感）+ elevation 投影，延续黑白中性主题（#2E2E2E 基调）。
- 图标：扁平极简线性（约 1.5dp 描边风格），新增两枚矢量图标：交换 ⇄、收起 ◄/►。
- 实现时出 2~3 版渐变/高光强度变体，装机真机挑选。

## 4. 设置变更清单

| 处置 | 设置项 |
|---|---|
| 新增 | 贴边方向（左/右）、长条厚度（默认 27dp）、长条长度占比（默认 75%）、长条圆角（默认 10dp）、工具栏"左右换边"按钮 |
| 沿用/映射 | 长条颜色/透明度/渐变（原把手三项沿用）、竖条宽/高、窗口边距、固定位置、字号、窗口透明度、自动收起、工具栏全套 |
| 废弃 | 把手宽/高（宽度映射为长条厚度后废弃）、握线四样式、半把手形态、感应区宽度、快速甩动开关 |

DataStore 旧 key 读取时做映射/忽略；**无数据库改动**（Room v3 不动）。

## 5. 架构方案

- **核心重构：侧别坐标系**。新增 Side（LEFT/RIGHT）概念，把所有位置数学
  （贴边、钳制、最大宽、展开锚定、收起落位、缩放手柄位置、IME 避让）收口到统一工具层，
  禁止散落各处的左锚定 if。
- 改动集中：FloatingNoteWindowView（布局重排 + 三段命中 + 新收起 + 形变动画）、
  FloatingBallView（贴边侧参数化）、FloatingNoteService（侧别状态 + 锚定/交换动画编排）、
  SettingsRepository（新 key + 映射）、窗口布局 XML 重写、SettingsScreen 整理。
- **不动**：两形态互斥状态机、「从哪来回哪去」锚定模型（泛化为按侧）、键盘优先策略、
  自制文本菜单、自动保存/自动收起、单进程架构。

## 6. 分期与验收

- **一期**：三段长条 + 箭头收起 + 贴窗无缝 + 窗口几何重算 + 旧设置映射（仅左模式）。
- **二期**：镜像系统全套（侧别参数化 + 交换键 + 竖条双侧 + 缩放手柄换角）。
- **三期**：形变动画 / 玻璃质感打磨 + 设置页更新 + 全量验收。

验收：MuMu 全链路（建/拖/收/换边/矮窗压缩/键盘上浮）+ 真机手感调优
（长条厚度与段占比的出厂默认值在此轮拍板）。

## 7. 待拍板（⚠）

~~1. 矮窗时两端保底 28dp，还是纯等比例压缩？~~ → **已拍板（2026-08-31）：两端保底 28dp**。
~~2. 默认项确认~~ → **已拍板（2026-08-31）：全部按默认定稿**——中段轻点 = 无动作、
   三段皆可按住拖动、握线废弃、动画约 250ms、侧别持久化 + 设置页「贴边方向」入口。

**设计已全部定稿，无待拍板项。**

## 8. 决策记录（本次推翻的旧拍板）

- M5.6「收起固定贴左」→ 双侧镜像（2026-08-31）。
- 2026-08-29 出厂默认「把手黑/70%/渐变100%/43×55」→ 长条参数体系，新默认值待真机调优拍板。
- v1.0.1「拖动指示器隐藏」留下的空壳代码 → 随目标区一并删除。
