# M2 里程碑验收说明

## 完成情况对照（M2 完成标准）

| 任务 | 状态 |
|---|---|
| 列表页：按 updatedAt 倒序，标题 / 摘要 / 相对时间 | ✅ 代码完成，待真机确认 |
| 深色模式跟随系统 | ✅ Compose 主题 + 深色窗口主题（防启动闪白），待真机确认 |
| 编辑页 + 1s 防抖自动保存 | ✅ 代码完成，待真机确认 |
| 字号使用 font_size_sp（默认 15sp） | ✅ 已接入（设置页 M5 才能改，当前固定默认值） |
| 新建（FAB）、删除（确认框） | ✅ 代码完成，待真机确认 |
| 保存状态显示（输入中… / 保存中… / 已保存 HH:mm） | ✅ 代码完成，待真机确认 |
| 标题为空时自动取正文前 20 字 | ✅ |
| 编译通过 | ✅ BUILD SUCCESSFUL |

新增文件：
- `ui/theme/Theme.kt`（深浅色主题）
- `ui/ViewModels.kt`（列表/编辑 ViewModel + 工厂）
- `ui/NoteListScreen.kt`（列表 + 空态 + FAB + 长按删除确认）
- `ui/NoteEditScreen.kt`（编辑 + 防抖保存 + 保存状态 + 删除）
- `manager/AutoSaveManager.kt`（泛型防抖，M4 悬浮窗复用）
- `util/TimeUtils.kt`（相对时间）、`util/NoteDisplay.kt`（标题/摘要派生）

实现说明：
- "点开新建又马上退出"会自动删除空笔记，列表不会积累空记录
- 旋转屏幕不丢输入（编辑状态保存在 ViewModel）
- 系统直接杀 App 时兜底保存（ViewModel.onCleared 同步落库）

## 手机验证步骤（约 3 分钟）

安装 `D:\FloatNote\app\build\outputs\apk\debug\app-debug.apk`（覆盖安装即可，数据不丢）：

1. **新建**：打开 App -> 空态提示 -> 点右下角「+」进入编辑页。
2. **自动保存**：输入标题和正文 -> 停止输入约 1 秒 -> 底部显示「已保存 HH:mm」。
3. **返回**：顶栏返回键回到列表 -> 笔记出现在列表（标题 + 摘要 + "刚刚"）。
4. **排序**：新建第二条并输入 -> 列表第一条变成它。
5. **删除**：长按列表项 -> 确认框 -> 删除生效；编辑页右上角删除同样有效。
6. **杀进程数据不丢**：编辑输入 -> 立即从最近任务划掉 App -> 重开 -> 内容还在。
7. **空笔记清理**：点「+」进编辑页但一个字不输 -> 返回 -> 列表里没有新空笔记。
8. **深色模式**：系统切深色 -> App 跟随变深色，无白底刺眼。

全部符合即 M2 验收通过。

## 编译命令

```bash
cd /d/FloatNote
export JAVA_HOME="D:\\java1"
cmd //c "gradlew.bat assembleDebug"   # 增量约 1 分钟
```
